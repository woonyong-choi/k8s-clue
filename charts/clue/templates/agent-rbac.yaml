apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "clue.fullname" . }}-agent
  labels:
    {{- include "clue.labels" . | nindent 4 }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "clue.fullname" . }}-agent-read-only
  labels:
    {{- include "clue.labels" . | nindent 4 }}
rules:
  - apiGroups: [""]
    resources: ["pods", "events", "services", "endpoints", "configmaps", "serviceaccounts", "resourcequotas"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["apps"]
    resources: ["deployments", "replicasets", "statefulsets", "daemonsets"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["discovery.k8s.io"]
    resources: ["endpointslices"]
    verbs: ["get", "list", "watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "clue.fullname" . }}-agent-read-only
  labels:
    {{- include "clue.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "clue.fullname" . }}-agent-read-only
subjects:
  - kind: ServiceAccount
    name: {{ include "clue.fullname" . }}-agent
    namespace: {{ .Release.Namespace }}

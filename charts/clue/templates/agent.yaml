apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: {{ include "clue.fullname" . }}-agent
  labels:
    {{- include "clue.labels" . | nindent 4 }}
    app.kubernetes.io/component: agent
spec:
  selector:
    matchLabels:
      {{- include "clue.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: agent
  template:
    metadata:
      labels:
        {{- include "clue.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: agent
    spec:
      serviceAccountName: {{ include "clue.fullname" . }}-agent
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: agent
          image: {{ include "clue.image" . | quote }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: ["python", "src/services/target/cluster-agent/app.py"]
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
          env:
            - name: MANAGEMENT_BASE_URL
              value: http://{{ include "clue.fullname" . }}.{{ .Release.Namespace }}.svc/api
            - name: AGENT_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ include "clue.fullname" . }}-bootstrap
                  key: AGENT_TOKEN
            - name: TARGET_CLUSTER_ID
              value: {{ .Values.agent.clusterId | quote }}
            - name: WORKSPACE_ID
              value: default
          resources:
            {{- toYaml .Values.agent.resources | nindent 12 }}
